import { GenerateBlogService } from "@wba/logic";

const main = async () => {
  // const blog = await GenerateBlogService.generate();
  // console.log(blog);
  // const published = await GenerateBlogService.publish(blog);
  // console.log(published.status, await published.text());
};

main();
